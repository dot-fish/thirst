{
  "name": "thirst.dot.fish",
  "version": "0.1.0",
  "private": false,
  "scripts": {
    "demo": "yarn run build && surge build --domain http://thirst.surge.sh",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "moment": "^2.20.1",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.1",
    "styled-components": "^3.1.6",
    "styled-normalize": "^4.0.0"
  }
}
